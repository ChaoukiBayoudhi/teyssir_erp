# Windows scripts must reach Windows with CRLF line endings (PowerShell 5.1 mis-parses
# here-strings/scripts with LF-only endings; cmd.exe prefers CRLF). git archive (GitHub ZIP)
# honours these too.
*.ps1 text eol=crlf
*.bat text eol=crlf
